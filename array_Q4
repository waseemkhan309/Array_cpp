// Q: count pairs with given sum in the array.
#include<iostream>
using namespace std;
int main(){
    int arr[6]={2,6,7,1,3,4};
    int sum ;
    cout<<"Enter target value : ";
    cin>>sum;
    
    for(int i=0; i<6; i++){
        for(int j=i+1; j<6; j++){
            if(arr[i]+arr[j] == sum ){
                cout<<i<<" "<<j<<endl;
            }
        }
    }
    return 0;
}


/*"Count pair with given sum in the array" means that you are given an array of integers and a target sum, and you need to find the number of pairs of integers in the array that add up to the target sum.

For example, suppose you are given the array [1, 2, 3, 4, 5] and the target sum 6. The pairs of integers in the array that add up to 6 are (1, 5), (2, 4), and (3, 3). So the number of pairs that add up to 6 is 3.
*/
